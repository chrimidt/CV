\documentclass[12pt,a4paper]{article}
\usepackage{graphicx} % Required for inserting images
\usepackage{titlesec}
\usepackage{natbib} % Imports biblatex package
\setlength\parindent{0pt}
\graphicspath{{images/}}
\usepackage{float}
\usepackage[margin=1in]{geometry}
\usepackage{url} % This enables the handling of URLs in the bibliography
\usepackage{amsmath}
\usepackage{hyperref}
\usepackage{tabularx}
\usepackage{placeins}
\usepackage{listings}
\usepackage{verbatim}
\usepackage{booktabs}
\usepackage{multirow}

\usepackage{xcolor} % Optional: For color syntax highlighting


\begin{document}

\section*{Slide 0 - Title}

Today we'll walk you through our project "Computer Vision - Multi-Label Image Classification: Query2Label and MLSPL". I'm Christine, presenting with Andreas and Riajul. We will go into why multi-label learning matters, outline two complementary methods - Query2Label for feature recognision from the paper "Query2Label: A Simple Transformer Way to Multi-Label Classification", and ROLE loss for sparse annotations from the paper "Multi-Label Learning from Single Positive Labels", and describe how we reproduced them on MS-COCO 2014 benchmark dataset, and summarise the results.

\section*{Background: Why loss functions matter}
Multi-label tasks differ from classic single-label classification because every image can contain several objects at once. That complicates training: the loss function must guide the network while coping with severe class imbalance and with labels that may simply be missing. Choosing that loss wisely is therefore crucial.

\section*{Losses covered}
In this study we focus on four families of losses.
Binary Cross-Entropy is the standard baseline.
Expected Positive Regularisation tries to nudge the model so that each image has roughly the expected number of positives.
ROLE—Regularised Online Label Estimation—alternates between updating the classifier and softly imputing missing labels.
Finally, Focal and its Asymmetric variant down-weight easy negatives so that scarce positives get a stronger voice.

\section*{Regularised Online Label Estimation (ROLE)}
In this project, we explore the case of minimal supervision: the single positive label setting, where only one relevant class label is observed
per image and the rest are unannotated. 

The authors of MLSPL introduce an approach for training with single positive labels called ROLE. Here the idea is to optimize the labels and the image classifier simultaneously during training (Expectation-maximization algorithm). 

The model learns to parameters: f and g. f is a standard image classifier where the output is a vector of per-class prediction based on an input image. g returns an estimated label vector for a given image ID n. g could return the appropriate row of differentiably parameterized label matrix. These estimated labels are used to supervise f. f is encouraged to predict observed positive labels correctly. 

Expected number of positive labels per image ($\kappa$). This is a single fixed value for a given dataset. $\kappa$ is used to define a regularisation term that penalizes f for predicting too many or too few positives in a batch.

g is updated using losses like those for f.

\section*{Binary Cross-Entropy refresher}
This is the most basic loss. It treats each label as separate and tries to predict whether it's present (1) or not (0).

Problem:

If many labels are missing, BCE treats them as 0s (negatives), even though we just don't know. This can confuse the model.

Also, since most labels are usually negative, the model might focus more on those and ignore the rare positives.

\section*{BCE limitations and Positive-only BCE}
To avoid false negatives, we train the model only on the labels we are sure about—the positives.
Problem: Now the model has no reason to predict zeros, so we need extra rules to control its behavior; a regularization, which brings us to EPR.

\section*{Expected Positive Regularisation}
EPR keeps the positive-only loss yet adds a batch-level penalty: we tell the network the domain prior $\kappa$, roughly how many labels an image should contain, and we punish the squared distance between $\kappa$ and the average number its own predictions switch on. 


\section*{ROLE}
ROLE goes one step further.
We train two models at once:

One is the usual classifier that predicts labels.

The other estimates the labels we don't see.

They learn from each other during training.
The idea is that the second model can guess missing labels softly (not just 0 or 1), and the main model uses those guesses to improve.

% \section*{Focal and Asymmetric Focal}
% In parallel, focal losses reshape the BCE curve so that easy negatives contribute very little, while hard, ambiguous examples dominate. The asymmetric version lets us pick one focusing parameter for positives and another for negatives—extremely useful when positives are both scarce and more informative.

\section*{Experimental setup}
Let me switch to implementation details.
We reproduced Q2L and MLSPL on two RTX 3060 GPUs.
For Q2L we matched the authors’ environment: Python 3.7, PyTorch 1.9 and minor patches such as rebuilding inplace-ABN and importing RandAugment.
For MLSPL, which is lighter, we used Python 3.11 and PyTorch 2.2 on a single GPU to guarantee repeatability.

\section*{Dataset and metric}
All experiments run on the MS-COCO 2014 benchmark—82 thousand training and 40 thousand validation images across 80 categories, each image resized to 448 or 576 pixels. Performance is reported in mean Average Precision, or mAP, the standard metric for multi-label classification.

\section*{Q2L backbones}
We evaluated six backbones inside the Q2L framework:
ResNet-101 at resolutions 448 and 576,
TResNet-L with and without ImageNet-22k pre-training,
and two transformer-style models: Swin-Large and CvT-w24.
Memory overflow forced us to omit Swin and CvT from scratch, so we fine-tuned their 22k-pre-trained weights.

\section*{MLSPL protocol}
For MLSPL we follow the paper exactly. Each image is reduced to a single positive label. We first train a linear classifier on frozen features for 25 epochs, grid-search learning rate and batch size, and then fine-tune end-to-end for another 10 epochs with a smaller learning rate.

\section*{Q2L results}
Here are the numbers. Our Q2L reproductions line up almost perfectly with the paper: 84.9 mAP on ResNet-101 at 448 pixels and up to 91.3 mAP with the CvT backbone. That validates both the original claims and our environment.

\section*{MLSPL results}
For MLSPL, the linear variant hits 66.3 mAP—identical to the paper—and fine-tuning nudges it to 66.9, slightly surpassing the reported 66.3. So even when each training image has only a single confirmed label, ROLE recovers roughly two-thirds of fully supervised performance on COCO.


\end{document}